<mvc:View controllerName="project.frotasapp.controller.Cargos"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc">
    <Page showHeader="false" showFooter="true">
        <VBox class="sapUiSmallMargin">
            <Title text="{i18n>titleCargos}" level="H3"/>
            <Table id="cargoTable" items="{path:'cargos>/', templateShareable: false}" fixedLayout="false" growing="true">
                <headerToolbar>
                    <Toolbar design="Transparent">
                        <SearchField search="onCargoFilter" width="{= ${device>/system/phone} ? '80%' : '30%' }" />
                        <ToolbarSpacer></ToolbarSpacer>
                        <Button icon="sap-icon://download" press="onDataExport" />
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="{i18n>columnId}"/>
                    </Column>
                    <Column minScreenWidth="Tablet">
                        <Text text="{i18n>columnDescricao}"/>
                    </Column>
                    <Column width="1rem">
                        <Text text=""/>
                    </Column>
                    <Column width="1rem">
                        <Text text=""/>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{cargos>Id}"/>
                            <Text text="{cargos>Descricao}"/>

                            <Button type="Ghost" icon="sap-icon://edit" onPress="" />
                            <Button type="Reject" icon="sap-icon://delete" onPress="" />

                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </VBox>
        <footer>
            <Toolbar>
                <ToolbarSpacer/>
                <Button type="Emphasized" icon="sap-icon://add" text="{i18n>btnAdicionar}" press="onAddPress" />
            </Toolbar>
        </footer>
    </Page>
</mvc:View>